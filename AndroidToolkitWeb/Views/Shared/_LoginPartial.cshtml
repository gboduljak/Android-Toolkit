@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        @*<ul class="nav navbar-nav navbar-right">

                  <li>
                      <a href="~/Account/Manage" title="Manage">
                          <i class="icon-user"></i>
                          Hello, <strong>@User.Identity.Name</strong>
                      </a>
                  </li>
                <li>
                    <a href="~/Home/Chat">
                        <i class="icon-comment"></i>
                        Chat Room
                    </a>
                </li>

                    @if (User.IsInRole("Admin"))
                    {
                       <li>
                           <a href="~/Admin/Panel">
                               <i class="icon-dashboard"></i> <strong>Admin Panel</strong>
                           </a>
                       </li>
                    }

                <li>
                    <a href="javascript:document.getElementById('logoutForm').submit()">
                        <span class="icon-logout"></span>
                    </a>
                </li>
            </ul>*@
        <ul class="nav pull-right">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Hello, <strong>@User.Identity.Name</strong> <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li>
                        <a href="~/Account/Manage" title="Manage">
                            <i class="icon-user"></i>
                            Hello, <strong>@User.Identity.Name</strong>
                        </a>
                    </li>
                    <li>
                        <a href="~/Home/Chat">
                            <i class="icon-comment"></i>
                            Chat Room
                        </a>
                    </li>

                    @if (User.IsInRole("Admin"))
                    {
                        <li>
                            <a href="~/Admin/Panel">
                                <i class="icon-dashboard"></i> <strong>Admin Panel</strong>
                            </a>
                        </li>
                    }

                    <li>
                        <a href="javascript:document.getElementById('logoutForm').submit()">
                            <span class="icon-logout"></span>
                        </a>
                    </li>
                </ul>
            </li>
        </ul>

    


    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
